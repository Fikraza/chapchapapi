const generate = require("./generate");
const template = require("./template");
const upload = require("./upload");
const uploadTX = require("./uploadTX");

module.exports = { generate, template, upload, uploadTX };
