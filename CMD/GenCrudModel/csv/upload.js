//upload csv
// will continue even if an item fails to upload or there
//is an error

async function uploadCSV({ csvRecord, req, index }) {
  // whatever the funnction returns
  // if object will be uploaded

  return null;
}

// the file can ruturn null instead of the
//function that means the default
module.exports = uploadCSV;
